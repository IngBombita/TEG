// const { Map, List } = require('immutable');

const initialState = {
  players: [],
  roundOrder: [],
  provinceCardsDeck: [],
  roundNumber: 1,
  currentPlayer: 0,
};

let gameState = initialState;

module.exports = initialState;
